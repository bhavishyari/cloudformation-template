pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name ecs---template-body file://ecs-service-td.yml --region 'us-east-2' --capabilities CAPABILITY_IAM"
              }
             }
            }
            }