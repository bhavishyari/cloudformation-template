pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name ecs-infra-creation --template-body file://ecs-infra-creation.yml --region 'us-east-2' --capabilities CAPABILITY_IAM"
              }
             }
            }
            }